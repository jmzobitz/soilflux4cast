name: Acquire NEON Env Data

on:
  workflow_dispatch:
    inputs:
      month:
        description: "Month to download (YYYY-MM)"
        required: true
        type: string

jobs:
  acquire-env:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install R package dependencies
        uses: r-lib/actions/setup-r-dependencies@v2

      - name: Run environment acquisition script
        run: |
          Rscript scripts/acquire_env.R "${{ github.event.inputs.month }}"
